// Package markers parses and renders the c3p managed-block markers
// (<!-- c3p:vN:begin --> ... <!-- c3p:vN:end -->) used to splice
// generated content into user-owned files (CLAUDE.md). F1 lands an empty
// scaffold; D3 (epic claude-code-profiles-wc1) fills it in against R44–R46.
package markers
